# hdhomerun.local (mDNS) does not resolve from inside a container on the
# default bridge network. Use your device's LAN IP instead (see README for
# how to find it).
HDHOMERUN_HOST=192.168.68.159
HDHOMERUN_PORT=80
